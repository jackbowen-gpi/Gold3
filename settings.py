try:
    # Prefer package settings (gchub_db/settings.py)
    # from gchub_db.settings import (
    #     SETTING_A,
    #     SETTING_B,
    # )  # replace with actual setting names
    pass
except ImportError:
    # Fallback: try loading legacy common settings if package import fails
    pass  # replace with actual setting names
